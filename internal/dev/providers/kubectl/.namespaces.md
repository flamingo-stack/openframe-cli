<!-- source-hash: c74918819ef6c7ca9e973b22c9d0293e -->
This file provides namespace discovery and validation functionality for Kubernetes clusters using kubectl commands.

## Key Components

- **GetNamespaces**: Retrieves all namespaces in the current cluster
- **ValidateNamespace**: Verifies if a specified namespace exists
- **FindResourceNamespace**: Intelligently locates the namespace containing resources matching a service name
- **searchResourceByType**: Internal helper that searches for specific resource types across all namespaces

## Usage Example

```go
// Get all available namespaces
namespaces, err := provider.GetNamespaces(ctx)
if err != nil {
    log.Fatalf("Failed to get namespaces: %v", err)
}

// Validate a specific namespace exists
err = provider.ValidateNamespace(ctx, "production")
if err != nil {
    log.Printf("Namespace validation failed: %v", err)
}

// Find which namespace contains resources for a service
namespace, err := provider.FindResourceNamespace(ctx, "my-service")
if err != nil {
    log.Fatalf("Failed to find service namespace: %v", err)
}
fmt.Printf("Service found in namespace: %s\n", namespace)
```

The `FindResourceNamespace` function is particularly useful for auto-discovery scenarios, searching deployments, statefulsets, and pods to locate where a service's resources are running. It defaults to the "default" namespace when no matching resources are found.