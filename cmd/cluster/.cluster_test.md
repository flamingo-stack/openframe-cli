<!-- source-hash: 94a1927783f5cb767318305eb7ab606c -->
This test file validates the cluster command functionality, specifically testing the root cluster command using the project's testing utilities.

## Key Components

- **`init()`** - Initializes test mode using `testutil.InitializeTestMode()`
- **`TestClusterRootCommand(t *testing.T)`** - Tests the root cluster command without any specific setup or teardown requirements

## Usage Example

```go
// Run the cluster command test
func TestClusterRootCommand(t *testing.T) {
    // Tests the "cluster" command using the GetClusterCmd function
    // No setup or teardown functions are provided (nil parameters)
    testutil.TestClusterCommand(t, "cluster", GetClusterCmd, nil, nil)
}
```

The test leverages the `testutil.TestClusterCommand` helper function to validate the cluster command behavior, passing the command name "cluster", the command getter function `GetClusterCmd`, and no setup/teardown functions. This ensures the basic cluster command structure and functionality work correctly.