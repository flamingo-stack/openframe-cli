<!-- source-hash: 1ebdb79f11ad431dc5abf5b36825a179 -->
Implements the `cluster status` command for the OpenFrame CLI, providing detailed information about Kubernetes cluster health and status.

## Key Components

- **getStatusCmd()** - Creates and configures the cobra command with flags and validation
- **runClusterStatus()** - Main execution logic that handles cluster selection and status display
- **Command flags integration** - Supports detailed output, app filtering, and verbose modes through global flag system
- **Interactive cluster selection** - Uses UI components for user-friendly cluster selection when no name provided

## Usage Example

```go
// Register the status command
statusCmd := getStatusCmd()
clusterCmd.AddCommand(statusCmd)

// Command supports various invocation patterns:
// openframe cluster status my-cluster
// openframe cluster status --detailed
// openframe cluster status my-cluster --no-apps
// openframe cluster status  // interactive selection
```

The command integrates with the service layer to retrieve cluster information and uses the operations UI for interactive cluster selection. It validates all flags before execution and wraps the main logic with common setup utilities for consistent error handling and configuration management.