<!-- source-hash: ab50e3470b38bf6094dd295fd6cac1cf -->
Note: The provided source appears truncated at the end (`retur`), but based on the visible logic, here's the documentation:

## Key Components

- **`getCreateCmd()`** — Builds the `cluster create` Cobra command, wiring flag validation (`PreRunE`) and execution (`RunE`) via `utils.WrapCommandWithCommonSetup`.
- **`runCreateCluster(cmd, args)`** — Main command handler. Resolves cluster configuration either through an interactive wizard (`ui.NewConfigurationHandler`) or directly from CLI flags/args when `--skip-wizard` is set, validates node/scaling flags, and dispatches to dry-run preview or actual cluster creation.
- **`cloudPlanPreview(ctx, config)`** (assigned to `planPreviewFn`) — Runs a real Terraform plan for cloud cluster types (GKE/EKS) to preview resource changes without provisioning, including cloud auth checks and cost estimation. Overridable for tests.
- **`showCostEstimate(...)`** — Best-effort monthly cost estimate using `infracost`; offers install/login flows interactively, falling back to a generic pricing hint (`ui.CostHint`) if unavailable.
- **`offerInfracostInstall()` / `offerInfracostLogin()`** — Interactive helpers that prompt the user to install `infracost` or run its browser-based login, attached directly to the terminal.
- **Test seams**: `infracostAvailableFn`, `infracostOfferFn`, `infracostLoginFn`, `planPreviewFn` — package-level function variables allowing unit tests to stub out real network/CLI/browser interactions.

## Usage Example

```go
// Registering the create command with a parent cluster command
func NewClusterCmd() *cobra.Command {
    clusterCmd := &cobra.Command{Use: "cluster"}
    clusterCmd.AddCommand(getCreateCmd())
    return clusterCmd
}
```

Typical CLI invocations handled by this file:

```bash
# Interactive selection menu
openframe cluster create

# Direct creation with defaults, no prompts
openframe cluster create --skip-wizard

# Cloud cluster (GKE) with explicit settings
openframe cluster create my-gke --type gke --project my-project --region us-central1 --skip-wizard

# Preview cloud resources/costs without provisioning
openframe cluster create my-eks --type eks --region us-east-1 --dry-run
```

This file lives at [`internal/cluster/create.go`](https://github.com/flamingo-stack/openframe-cli/blob/main/internal/cluster/create.go) in the `cluster` package.