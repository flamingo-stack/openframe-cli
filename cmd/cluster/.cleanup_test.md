<!-- source-hash: 3e45e22e584753e99e05d1da22fb5e7a -->
Test file for the cluster cleanup command functionality, validating the cleanup command behavior using mock execution.

## Key Components

- **TestCleanupCommand**: Main test function that validates the cleanup command using the test framework
- **setupFunc**: Configuration function that initializes a mock executor for testing
- **teardownFunc**: Cleanup function that resets global flags after test execution
- **init()**: Package initialization that enables test mode

## Usage Example

```go
// Run the cleanup command test
func TestCleanupCommand(t *testing.T) {
    // Setup mock executor
    setupFunc := func() {
        utils.SetTestExecutor(testutil.NewTestMockExecutor())
    }
    
    // Reset state after test
    teardownFunc := func() {
        utils.ResetGlobalFlags()
    }
    
    // Execute the test using the framework
    testutil.TestClusterCommand(t, "cleanup", getCleanupCmd, setupFunc, teardownFunc)
}
```

The test uses the `testutil.TestClusterCommand` framework to verify that the cleanup command executes properly with mocked dependencies, ensuring isolated and predictable test behavior.